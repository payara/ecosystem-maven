# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Verify PR

on:
  pull_request:
    branches: [ master ]
    paths-ignore:
      - 'README.*'
      - '**/README.*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        distribution: zulu
        java-version: 11

    - name: Cache repository
      uses: actions/cache@v2
      with:
        path: |
          ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Clean snapshot dependencies
      run: "test -d ~/.m2/repository && find ~/.m2/repository/ -name '*-SNAPSHOT' -type d -print0 | xargs -0 rm -r"
      continue-on-error: true

    - name: Build payara-maven-plugins-common
      env:
        MAVEN_OPTS: -Dmaven.wagon.httpconnectionManager.ttlSeconds=25 -Dmaven.wagon.http.retryHandler.count=3 -Dmaven.wagon.rto=30000 -Djava.net.preferIPv4Stack=true
      working-directory: ./payara-maven-plugins-common
      run: mvn -B -V clean install

    - name: Build payara-cloud-maven-plugin
      env:
        MAVEN_OPTS: -Dmaven.wagon.httpconnectionManager.ttlSeconds=25 -Dmaven.wagon.http.retryHandler.count=3 -Dmaven.wagon.rto=30000 -Djava.net.preferIPv4Stack=true
      working-directory: ./payara-cloud-maven-plugin
      run: mvn -B -V clean install

    - name: Build payara-micro-maven-archetype
      env:
        MAVEN_OPTS: -Dmaven.wagon.httpconnectionManager.ttlSeconds=25 -Dmaven.wagon.http.retryHandler.count=3 -Dmaven.wagon.rto=30000 -Djava.net.preferIPv4Stack=true
      working-directory: ./payara-micro-maven-archetype
      run: mvn -B -V clean install

    - name: Build payara-micro-maven-plugin
      env:
        MAVEN_OPTS: -Dmaven.wagon.httpconnectionManager.ttlSeconds=25 -Dmaven.wagon.http.retryHandler.count=3 -Dmaven.wagon.rto=30000 -Djava.net.preferIPv4Stack=true
      working-directory: ./payara-micro-maven-plugin
      run: mvn -B -V clean install -DskipTests

    - name: Archive logs on failure
      if: failure()
      uses: actions/upload-artifact@v2
      with:
        name: logs
        path: "**/*.log"